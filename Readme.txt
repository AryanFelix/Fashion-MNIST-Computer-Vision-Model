#Aryan Felix

#Documentaion present inside the program

1. To try the model out with different clothing articles replace '0' with any other value in the train_set[0], train_label[0], test_set[0] and test_label[0].
2. To further increase the accuracy of the model, increase the number of neurons (from 128 to 1024 maybe).
3. Increase the number of epochs for better training.